git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

git config --global user.name "DevikaPGowda"
git config --global user.email "devikasureha@gmail.com" 


git clone <link>

git add .  -> staging
git commit -m "commit message"   -> to commit
git push   -> push to remote

git fetch  -> to compare with remote for syncing
git pull   -> get the remote data to local 